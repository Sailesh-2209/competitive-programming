#include <bits/stdc++.h>

using namespace std;

void solve() {
	string a, b; cin >> a >> b;
	int n = (int) a.size();
	int m = (int) b.size();
	int i = 0, j = 0;
	
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	#ifdef LOCAL
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	#endif
	int t; cin >> t;
	while (t--) {
		solve();
	}
	return 0;
}